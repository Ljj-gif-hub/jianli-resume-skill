---
name: jianli
description: >
  针对目标面试岗位（或具体公司的 JD），为当前项目（任意项目）生成定制中文简历。
  自动"指纹识别"项目的技术栈/领域/规模/你的贡献（git 提交与文档），按岗位矩阵或
  JD 定制技能栏、项目亮点与自我评价，产出 md + HTML + PDF 三件套
  （经典/双栏/极简/现代四套版式模板，关键词切换；PDF 自动打开预览），
  可选产出 BOSS 直聘文案与面试准备题。安装到用户级技能目录后，任意项目启动的会话都能用。
  触发词："生成简历"、"写简历"、"帮我搞份简历"、"/jianli"、"审查简历"、"检查简历"。
  用法：/jianli <目标岗位>，如 /jianli 后端开发、/jianli Vue前端、/jianli AI应用开发。
  JD 模式：/jianli <公司 JD>（粘贴 JD 文本 / 文件路径 / 一句对方要求），每家公司
  JD 各生成一份对应简历。岗位层级不限（实习/校招/社招）。
  不要用 /resume：那是 Claude Code 内置的恢复会话命令，在 VS Code 里会报
  "/resume isn't available in this environment"。
---

# 简历生成技能

> 安装位置：用户级 `~/.claude/skills/jianli/`（Windows 即 `C:\Users\<你>\.claude\skills\jianli\`）
> 或项目级 `<项目>/.claude/skills/jianli/`。用户级安装后**任意目录启动的会话都能用 `/jianli`**。
> 详见 README 的「安装」一节。

针对目标岗位（或具体公司 JD），基于当前仓库的**真实代码、提交历史与文档**，生成一份可溯源、不夸大的中文简历。技能本身通用：任何项目都能先识别、再定制。

## 工作流程

1. **确认输入** — 岗位 / JD 与个人信息
2. **解析 JD（如提供）** — 按 `references/jd-tailoring.md` 提取要求清单
3. **识别项目** — 指纹分析当前仓库（任何项目通用）
4. **提取贡献** — 从 git 与文档还原你本人做的工作
5. **定向** — 有 JD 按 JD 定制，否则按岗位矩阵
6. **撰写输出** — 生成 md + HTML + PDF（`简历-<岗位>.md`，带公司则 `简历-<公司>-<岗位>.md`）
7. **自校验** — 事实核对 + PDF 校验，杜绝编造

---

## Step 1 确认输入

- **岗位 / JD**：来自调用参数。
  - 参数是岗位名（如 `/jianli 后端开发`）→ 走岗位矩阵（Step 5 无 JD 分支）。
  - 参数是具体公司 JD（粘贴文本 / 文件路径 / 口头描述对方要求）→ 以 JD 为准，走 JD 定制（Step 2 + Step 5 有 JD 分支）。
  - 都没有（只说"生成简历"）时，用 AskUserQuestion 问清楚：目标岗位、层级（实习/校招/社招），以及是否有一家公司的 JD。
  - 参数是"审查"（如 `/jianli 审查 简历-xxx.md`，或只说"检查/审查简历"）→ 不生成，直接走 Step 8 审查模式。
- **多公司**：用户给了好几家公司 JD → 每家各生成一份简历（Step 6），共用同一批项目证据。
- **渠道**：投 BOSS 直聘表单 vs 附件简历，格式不同（前者要短文案，后者要完整 md）。
- **个人信息**：姓名/电话/邮箱/学历/所在地/可到岗时间。未提供一律用【占位】交给用户填。
- **复用现有资产**：先看当前项目根目录有没有 `简历-*.md`、`BOSS*.md`、`resume.html`、`_resume_assets/` 等既有简历文件。有就**先读**——复用版式结构、既有成熟表述，避免重复劳动；曾用本 skill 生成过简历的项目尤其适用。
- **资料库优先**：再读 `简历-资料库.md`（跨岗位共享的个人信息与事实源，格式见 writing-guide）。存在则占位符优先用它填，缺什么才问用户；本次新确认的信息在 Step 7 回写。
- **资料库隐私兜底**：创建资料库**前**必须检查项目 `.gitignore` 有没有 `*简历*` 排除规则——资料库含真实电话邮箱，没有这条规则就**先补上再创建**（连同 `*简历*.pdf/.html/.png`），防止敏感信息被提交；规则已存在的仓库不用重复加。

## Step 2 解析 JD（仅当提供了具体公司 JD）

严格按 `references/jd-tailoring.md` 解析：

- 提取：公司/业务背景、职位名称与层级、硬性要求、技术栈关键词、职责项、加分项、软性要求
- 产出"**JD 响应清单**"：JD 每条硬性要求 → 对应项目里的证据，或标注"未覆盖"
- 后续定向以 JD 为准，`positions.md` 岗位矩阵只用于判断岗位大类
- **覆盖率前置预警**：解析完先估"硬性要求 → 证据"命中率。若核心硬性要求（学历门槛 / 关键技术栈 / 经验年限）**全部无证据**，或命中率不足一半，**先停下来**向用户说明：哪些要求撑不起、当前项目与 JD 的差距、继续投的风险——用户确认"继续（接受薄简历 / 未覆盖标注）"才动笔，不要硬出一份单薄简历

## Step 3 识别项目

严格按 `references/project-analysis.md` 指纹分析，**不要跳过**——简历里每一个论断都必须来自这里的分析结果：

- 技术栈：检测 manifest（package.json / pom.xml / requirements.txt / go.mod / *.csproj ...）+ 目录结构
- 领域：README 首段 + 业务模块名，一句话概括"产品是什么、给谁用"
- 规模：grep 统计 Controller/Service/Repository/路由/页面/组件/测试的真实数量
- 部署与工程：Dockerfile / docker-compose / CI / scripts / 上线链接 / 监控
- 兜底：识别不出（空仓库/新项目）就停止猜测，直接问用户技术栈与自己负责的部分

## Step 4 提取你的贡献

- `git config user.name` 确认你的提交身份 → `git log --author=<身份> --oneline -50` 拉你的提交
- 从提交信息与 CHANGELOG / DEPLOYMENT_* / FIXES_* 文档提炼你主导的功能与**线上排障**（排障是简历金矿）
- **若仓库是多协作者**：`git shortlog -sne` 看作者分布，向用户确认工作边界后只写自己亲手做的，绝不把团队工作写进简历
- git 历史为空时，直接询问用户已做的工作
- JD 模式下，同一批证据用于所有公司的简历，但每份的强调/排序/措辞不同

## Step 5 定向

**有 JD** → 按 `references/jd-tailoring.md` 定制：
- 列硬性要求响应表，逐条找证据（有就写，没有明确标注"未覆盖"）
- 技术栈交集提到最前、补集不写；JD 关键词自然分布；职责映射到 bullet 并按 JD 优先级排序；公司业务挂钩

**无 JD** → 按 `references/positions.md` 岗位矩阵，确定四件事：
- **核心卖点**：该岗位最想看到什么 → 放最前、最醒目
- **证据清单**：去代码里挖哪些具体东西支撑卖点
- **弱化项**：哪些亮点对这个岗位没用 → 压掉篇幅
- **措辞**：岗位关键词（Agent / 高并发 / 兼容性 / 用例设计 ...）自然分布到全文

一个岗位通常命中"1 个主画像 + 1 个辅助画像"（如 后端+AI、前端+全栈）。不在矩阵内的岗位走"通用兜底画像"：按 JD 关键词逐项找证据，有就写、没有明确不写。

## Step 6 撰写输出

按 `references/writing-guide.md` 的模板与写作原则撰写：

- 结构：个人信息 / 教育背景（应届·校招·实习默认保留，社招可省，模板里自带该板块）/ 相关技能 / 项目经历 / 自我评价
- 项目经历用亮点 bullet：做了什么 + 怎么做 + 结果/规模
- 技术栈排序以岗位（或 JD 技术栈）为核心，辅助技术压后或并入"辅助扩展"一行
- **模板选择**：调用参数带版式关键词时切换模板——`双栏/侧栏`→`references/templates/template-sidebar.html`、`极简/黑白/学术/外企`→`template-minimal.html`、`现代/彩色/渐变/设计感`→`template-accent.html`、不带关键词或`经典/商务`→内置 `resume-template.html`（经典单栏）。关键词认不出（如"好看点的"）→ AskUserQuestion 列四套让用户选。矩阵见 `references/templates/README.md`；所有模板共享同一契约（@page 615.1pt×870pt、body 820×1160、tier 密度档、fit 脚本），换模板不影响单页保证
- 文件名：岗位模式写 `简历-<岗位>.md`；JD 模式写 `简历-<公司>-<岗位>.md`，多公司各一份。写入当前项目根目录或用户指定路径
- **覆盖前备份**：目标产物（md / html / pdf / png 整套）已存在时，先整套复制到 `简历备份/<YYYYMMDD-HHmm>-<岗位>/` 再覆盖——改坏可回退；备份目录内文件已被 .gitignore 的 `*简历*` 规则按文件名排除

### PDF 产出（标准交付，不是可选项）

1. **渲染 HTML**：产出 `resume-<岗位>.html`（JD 模式 `resume-<公司>-<岗位>.html`）。优先以当前项目根目录已有的 `resume.html` 为基底，套用其 CSS 与 `_resume_assets/` 素材填内容；项目没有模板时按"模板选择"规则选内置模板。头像/图标用项目素材或【占位】。
2. **单页保证**：简历必须恰好一页，按 `references/writing-guide.md` 的"单页优先"三道闸执行——① 内容超容量先砍弱化项；② 仍饱满给 `<body>` 加密度档 class（`tier-compact` / `tier-dense`）；③ 最后靠模板自带 fit 脚本自动缩放兜底。**复用项目 resume.html 时，务必把内置模板底部那段 fit `<script>` 整段注入到 `</body>` 前**，并核对 `@page` 尺寸与 body 画布一致（@page 615.1pt×870pt ↔ body 820×1160），否则仍会溢到第二页。
3. **转 PDF + PNG**：调用 skill 目录下 `references/pdf_build.ps1`：
   `powershell -ExecutionPolicy Bypass -File <skill>/references/pdf_build.ps1 -InputHtml <html> -OutputPdf <pdf>`，
   输出 `简历-<岗位>.pdf` 与同名 `.png`（脚本打印 `PDF_OK` / `PNG_OK`）。
4. **打开预览**：`Start-Process <pdf>` 给用户看。
5. **校验排版只读 PNG，禁止 Read PDF**：火山 GLM / 豆包等只接受 `text` / `image` / `tool_reference`。
   `Read` `.pdf` / `.docx` 会变成 `content.type=document`，直接 400。
   看一页是否贴底、是否溢出：`Read` 脚本打出的 PNG。不要 `Read` PDF。

## 可选伴生产物（写完问用户要不要）

- **BOSS 直聘文案**：提炼 ~200 字表单粘贴版 + ~400 字分点长版，回扣岗位卖点与出勤承诺；JD 模式回扣 JD 关键词
- **面试准备题**：针对简历每个亮点生成"它为什么是亮点 / 追问会问什么 / 怎么答"的模拟题

## Step 7 自校验（必做，交付前逐条核对）

- [ ] 每个技术名词 / 数量 / 链接都能 grep 溯源到当前项目
- [ ] 没有写项目里不存在的功能或技术（尤其"高并发""千万级""分布式事务"这类）
- [ ] 量化数据真实；没把握的写范围（"9+ 业务模块"）而非编造精确数
- [ ] 团队项目只含本人工作，时间线真实
- [ ] **JD 模式额外**：JD 每条硬性要求都在简历里有对应，或已在交付说明里明确标注"未覆盖"；不为了贴合 JD 编造任何技术/经验
- [ ] **多公司额外**：每份简历的措辞/排序确实不同，检查没把 A 公司的业务/技术句子写进 B 公司
- [ ] **PDF 额外**：HTML 内容与 md 一致（含【占位】提醒）；脚本校验 PDF 非空（<1KB 会失败），生成后确认路径与大小，并已打开预览
- [ ] **单页额外**：用 PNG（`Read` 图片）确认恰好一页、字号清晰（≥12px）、底部留白合适（内容不贴页底）；**禁止 Read PDF**。若超页或过密，回头按"砍内容 → 换密度档 → 确认 fit 脚本已注入"重出，而不是继续缩小字号硬塞
- [ ] **回写资料库**：本次用户新确认的个人信息 / 事实（如学校、城市）已更新进 `简历-资料库.md`，下次生成免重问
- [ ] 个人信息优先取自资料库；资料库没有的用【】占位并提醒用户替换、逐条确认
- [ ] 提醒用户：简历里每一点自己都要能讲清楚
- [ ] 生成的面试材料**不提交进公开 Git 仓库**；公开仓库则提示用 .gitignore 排除或放本地

## Step 8 简历审查模式（`/jianli 审查 [文件]`）

参数是"审查 / 检查简历"（可带 md / html / pdf 路径；不带则取项目里最新的 `简历-*.md`，拿不准就问用户）→ **只诊断不重写**，按三维度出问题清单，用户确认后再改：

1. **版式**：没有同名 PNG 先用 `pdf_build.ps1` 打一张，`Read` PNG 检查：恰好一页、字号 ≥12px、底部留白、有无溢出 / 截断（**禁止 Read PDF**）
2. **真实性**：逐条 grep 溯源技术名词 / 数量 / 链接，标出无法溯源或存疑的论断（对照 writing-guide 的真实性红线）
3. **表达**：对照 bullet 模板查——被动词（负责 / 参与 / 协助）、无证据空话（"精通""学习能力强"）、缺量化数字、同一技术词跨条重复

- **范围限制**：只能审查有 md / HTML 源的简历。外部 PDF 无法渲染校验（无头浏览器截 PDF 只得到空白图，已实测）——收到的第三方 PDF 请让用户给 md / HTML 源，或明确说明"仅凭口述无法校验版式"

输出：按严重度排序的问题清单（每条给位置 + 问题 + 改法建议）→ 用户挑选要改哪些 → 按 Step 6 重出受影响部分并重新过 Step 7。
